# Helm chart: file-storage-service — TODO

Скопировать структуру из `deploy/helm/api-server` (Chart.yaml, values.yaml,
templates/deployment.yaml, templates/service.yaml), заменить:
- имя чарта/приложения на `file-storage-service`
- порт на соответствующий из docker-compose.yml
- image.repository на `simensnx/file-storage-service`

См. ROADMAP.md, Phase 0.
