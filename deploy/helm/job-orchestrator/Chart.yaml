apiVersion: v2
name: job-orchestrator
description: |
  SimensNX job-orchestrator — владелец состояния задач в PostgreSQL, retry-цикл
  (queued -> processing -> done|failed -> retry|dead_letter). СТРОГО 1 реплика,
  см. values.yaml и docs/SCALING.md.
type: application
version: 0.1.0
appVersion: "0.1.0"
