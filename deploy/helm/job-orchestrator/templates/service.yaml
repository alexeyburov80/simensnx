apiVersion: v1
kind: Service
metadata:
  name: job-orchestrator
spec:
  selector:
    app: job-orchestrator
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
