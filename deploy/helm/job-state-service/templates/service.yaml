apiVersion: v1
kind: Service
metadata:
  name: job-state-service
spec:
  selector:
    app: job-state-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
